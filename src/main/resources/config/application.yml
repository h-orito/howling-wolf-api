spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sandboxdb?characterEncoding=UTF-8
    username: sandboxuser # 本番は起動引数で与える
    password: sandboxpass # 本番は起動引数で与える
    driverClassName: com.mysql.jdbc.Driver
  messages:
    basename: messages
    cache-duration: -1
    encoding: UTF-8
  jackson:
    property-naming-strategy: SNAKE_CASE
server:
  port: 8086
  servlet:
    context-path: /wolf4busy
    session:
      timeout: 86400
logging:
  level:
    org.springframework: INFO
    root: INFO
    org.seasar.dbflute: ERROR
  file: /var/log/wolf4busy/tomcat.log
  