-- partition table fk delete

alter table MESSAGE drop foreign key FK_message_player;
alter table MESSAGE drop foreign key FK_message_village_day;
alter table MESSAGE drop foreign key FK_message_village_player;
alter table MESSAGE drop foreign key FK_message_village_player_to;
alter table MESSAGE drop foreign key FK_message_message_type;
alter table MESSAGE drop foreign key FK_message_face_type;

-- partition

-- message
alter table MESSAGE partition by list (village_id) (
	partition p0 values in (1),
	partition p1 values in (2),
	partition p2 values in (3),
	partition p3 values in (4),
	partition p4 values in (5),
	partition p5 values in (6),
	partition p6 values in (7),
	partition p7 values in (8),
	partition p8 values in (9),
	partition p9 values in (10),
	partition p10 values in (11),
	partition p11 values in (12),
	partition p12 values in (13),
	partition p13 values in (14),
	partition p14 values in (15),
	partition p15 values in (16),
	partition p16 values in (17),
	partition p17 values in (18),
	partition p18 values in (19),
	partition p19 values in (20),
	partition p20 values in (21),
	partition p21 values in (22),
	partition p22 values in (23),
	partition p23 values in (24),
	partition p24 values in (25),
	partition p25 values in (26),
	partition p26 values in (27),
	partition p27 values in (28),
	partition p28 values in (29),
	partition p29 values in (30),
	partition p30 values in (31),
	partition p31 values in (32),
	partition p32 values in (33),
	partition p33 values in (34),
	partition p34 values in (35),
	partition p35 values in (36),
	partition p36 values in (37),
	partition p37 values in (38),
	partition p38 values in (39),
	partition p39 values in (40),
	partition p40 values in (41),
	partition p41 values in (42),
	partition p42 values in (43),
	partition p43 values in (44),
	partition p44 values in (45),
	partition p45 values in (46),
	partition p46 values in (47),
	partition p47 values in (48),
	partition p48 values in (49),
	partition p49 values in (50),
	partition p50 values in (51),
	partition p51 values in (52),
	partition p52 values in (53),
	partition p53 values in (54),
	partition p54 values in (55),
	partition p55 values in (56),
	partition p56 values in (57),
	partition p57 values in (58),
	partition p58 values in (59),
	partition p59 values in (60),
	partition p60 values in (61),
	partition p61 values in (62),
	partition p62 values in (63),
	partition p63 values in (64),
	partition p64 values in (65),
	partition p65 values in (66),
	partition p66 values in (67),
	partition p67 values in (68),
	partition p68 values in (69),
	partition p69 values in (70),
	partition p70 values in (71),
	partition p71 values in (72),
	partition p72 values in (73),
	partition p73 values in (74),
	partition p74 values in (75),
	partition p75 values in (76),
	partition p76 values in (77),
	partition p77 values in (78),
	partition p78 values in (79),
	partition p79 values in (80),
	partition p80 values in (81),
	partition p81 values in (82),
	partition p82 values in (83),
	partition p83 values in (84),
	partition p84 values in (85),
	partition p85 values in (86),
	partition p86 values in (87),
	partition p87 values in (88),
	partition p88 values in (89),
	partition p89 values in (90),
	partition p90 values in (91),
	partition p91 values in (92),
	partition p92 values in (93),
	partition p93 values in (94),
	partition p94 values in (95),
	partition p95 values in (96),
	partition p96 values in (97),
	partition p97 values in (98),
	partition p98 values in (99),
	partition p99 values in (100)
);
